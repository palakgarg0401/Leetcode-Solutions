// Complement of Base 10 Integer

class Solution {
    public int bitwiseComplement(int n) {
        if(n == 0) return 1;
        int num = n;
        int count = 0;
        while(n > 0){
            count++;
            n /= 2;
        }

        return ((1 << count) - 1) - num;
    }
}